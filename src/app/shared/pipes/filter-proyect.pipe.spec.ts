import { FilterProyectPipe } from './filter-proyect.pipe';

describe('FilterProyectPipe', () => {
  it('create an instance', () => {
    const pipe = new FilterProyectPipe();
    expect(pipe).toBeTruthy();
  });
});
